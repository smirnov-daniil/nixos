- When verifying Herdr upgrades in /home/smirnovd/flake, a previously running Herdr server can cause `protocol_mismatch` between the new client and old socket protocol. For clean checks, use a fresh `HERDR_SOCKET_PATH` and fresh XDG dirs, or stop the old server first.
- Pi-owned implementation is co-located under packages/pi/: the main agent, roles, extensions, prompts, skills, pi-review patch, patched pi-subagents package, and Pi-safe skillopt-sleep package/tests. Public outputs remain pi, pi-subagents, and skillopt-sleep. Keep flake inputs/locks global, package selection in packages/environment.nix, standalone tuicr/Herdr outside, project runtime state under .pi/, and graphify-out/ at repository root.
